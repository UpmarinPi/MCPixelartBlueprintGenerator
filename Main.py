# (input path: string) (output mode: int or string) (output path: string) (detail mode: 0 or 1)

#input path: 初期状態の画像
#output mode: 出力のモード
#output path: 出力するフォルダ位置。初期値は"./Outputs"
#detail mode: 詳細モードをtrueとするか。初期値はfalse。true時は色の数に 明・暗 が追加され、それらを指定する追加ファイル or 表示形式になる
